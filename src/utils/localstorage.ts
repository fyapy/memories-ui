// export const buildStorage = (name: string) => ({
//   get: () => localStorage.getItem(name),
//   has: () => !!localStorage.getItem(name),
//   remove: () => localStorage.removeItem(name),
//   set: (payload: string) => localStorage.setItem(name, payload),
// })
